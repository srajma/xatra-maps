[project]
name = "maps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "xatra",
]

[tool.uv.sources]
xatra = { path = "../xatra2", editable = true }
